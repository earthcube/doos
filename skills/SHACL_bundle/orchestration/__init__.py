"""Orchestration package for the decoder skill bundle.

Holds the LangGraph driver and shared helpers (e.g. the OpenRouter LLM client)
that the LLM-driven stages (1, 4, 5, 6) import. See PLAN.md §7.
"""
