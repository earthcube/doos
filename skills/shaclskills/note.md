OK, now let's work with this structure and give it more explicate directive.  In some cases I think the plan will be to make code, or SHACL, in other cases it
  might be pure LLM play for now.   Here are my thoughts on each step.   use this to expand the PLAN.md file.  Note that many of these steps need to pass data to
  a following step, so assets will likely need to stored in something like temporary files that are shared.  Though if there are other approaches that are better
  explore those.

  1) This is a pure LLM to structured data play.  For this, let's have this skill work flow start with a URL that will be inspected and have information about its
  location, name, description and keywords extracted.  The URLs will be hosting datasets, so any additional information about the dataset can be extracted.  Try
  to be consistent at this stage for what is extracted.

  2) This is the convert to RDF stage.  Here I would like to leverage schema.org vocabulary and the type Dataset.  Align to the types and properties from that.
  The primary resource you should reference is https://schema.org/Dataset

  3) Next is validation and this should be done leveraging pyshacl and the SHACL shape ‘googleRecommended.ttl’ in the assets directory of this step

  4) This step should use the validation output to generate a report.  This report should be structured in a way that explains the issue and perhaps a potential
  fix in a way that can be used by the next skill which will be making fixes

  5) This is the fix or repair stage.  Use the validation and the validation report to attempt to fix the RDF generated by stage 2 skill.   At this point the
  system should loop back to stage 3 to have the repair validated and then on to stage 4 and back to this stage.  If the result of stage 3 and 4 is that there is
  no repairing that needs to be done, the system can move on to stage 6.

  6) Getting to this stage means the RDF has been generated and validated.  This skill/stage should attempt to make a short write up of the events that took place
  to get to this stage.  Attempt to make a skill that will leverage RAiD (ref: https://metadata.raid.org/en/v1.6/) to make a strcutured record of what occured.
  RAiD is not a perfect fit here, so do the best you can and I will revisit this later.

  So each of these skills should be an independant atomic action.  However, these skills do need to be run in a sequence and information shared between them.
  Likely the first way to approach this is langchain/langraph so explore building a langraph code that does that sequencing and make doing this part of the
  PLAN.md file we will edit based on this prompt.


