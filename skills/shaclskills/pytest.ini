[pytest]
testpaths = tests
addopts = -q
# Silence third-party deprecation noise from rdflib/pyshacl (not our code).
filterwarnings =
    ignore::DeprecationWarning:rdflib
    ignore::DeprecationWarning:rdflib.graph
    ignore::DeprecationWarning:pyshacl
