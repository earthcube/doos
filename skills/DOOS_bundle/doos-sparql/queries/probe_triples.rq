# Sample any triples (default + named graphs via union of patterns).
# Params: LIMIT
PREFIX rdf: <http://www.w3.org/1999/02/22-rdf-syntax-ns#>

SELECT ?s ?p ?o
WHERE {
  {
    ?s ?p ?o .
  }
  UNION
  {
    GRAPH ?g {
      ?s ?p ?o .
    }
  }
}
LIMIT {{LIMIT}}
