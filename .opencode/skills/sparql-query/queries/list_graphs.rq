# Named graphs present in the store with approximate triple counts.
# Params: LIMIT
SELECT ?g (COUNT(*) AS ?tripleCount)
WHERE {
  GRAPH ?g {
    ?s ?p ?o .
  }
}
GROUP BY ?g
ORDER BY DESC(?tripleCount)
LIMIT {{LIMIT}}
