PREFIX schema: <https://schema.org/>
PREFIX skos: <http://www.w3.org/2004/02/skos/core#>
PREFIX shema: <http://schema.org/>

SELECT * WHERE {
  ?s schema:variableMeasured ?vm .
  ?vm schema:minValue ?min .
  ?vm schema:maxValue ?max .
}

